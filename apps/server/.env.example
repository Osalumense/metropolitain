PORT=4001
FRONTEND_ORIGIN=http://localhost:3000
TICK_INTERVAL_MS=4000

# Not used yet — mock ingestion doesn't call these. Real IDFM/DeepL wiring
# comes once we have live keys.
PRIM_API_KEY=
DEEPL_API_KEY=
