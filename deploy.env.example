# Copy this file to deploy.env (gitignored — real infra details never get committed)
# and fill in your own VPS. The Makefile reads it for every deploy/ssh/logs command.

VPS_HOST=root@your-vps-ip-or-hostname
VPS_PATH=/app/metropolitain
SSH_KEY=~/.ssh/id_ed25519
